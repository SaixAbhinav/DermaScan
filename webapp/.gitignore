# Python
__pycache__/
*.pyc
.venv/
venv/

# Model artifacts are large / generated by the training notebook.
# Keep the directory but ignore the heavy files (see model/README.md).
model/*.keras
model/*.h5
model/*.weights.h5
model/*.json
model/*.png
model/examples/
